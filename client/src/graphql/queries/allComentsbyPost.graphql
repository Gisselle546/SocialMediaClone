query commentsbypost($postId:Int!){
    commentsbypost(data:{postId:$postId}){
         id
        description,
        user{
            id,
            avatar,
            username
        },
       
    }
}