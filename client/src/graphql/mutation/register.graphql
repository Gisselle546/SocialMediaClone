mutation register($name:String!,$email:String!,$username:String!,$password:String!){
    register(data:{name:$name,email:$email,username:$username,password:$password}){
        accessToken,
        user{
            id
            username
            avatar
        }
        
            
        }
    }

